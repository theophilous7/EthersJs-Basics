const ethers = require("ethers");
const fs = require(fs - extra);
require("dotenv").config();

async function main() {
  //instead of writing the code i will just make a reference to the
  //time stamp of the course, Better Private Key Management. TimeStamp: 7:40:
}

main()
  .then(() => process.exit(0))
  .catch((error) => {
    console.error(error);
    process.exit(1);
  });
